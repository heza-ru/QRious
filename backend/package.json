{
  "name": "qrious-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun build src/server.ts --outdir dist",
    "start": "bun run dist/server.js"
  },
  "dependencies": {
    "hono": "^3.11.7",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.2.2"
  }
}

